/*Pointer is a variable used to store an address of an data 
It has 2 types 
1.Data varaible        int x=10;
2.Address varaible     int *p;
                       p=&x; //it having the address of x, not the value of x.
                       
 declaration        int *p;
 intialization      p=&x;
 dereferencing      cout<<*p; //going to the reference and accessing the data 
 
 */
